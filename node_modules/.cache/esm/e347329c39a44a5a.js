let express,dotenv;_39a‍.w("express",[["default",["express"],function(v){express=v}]]);_39a‍.w("dotenv",[["default",["dotenv"],function(v){dotenv=v}]]);


dotenv.config();
const app = express();
app.use(express.json());

app.get("/", (req, res) => {

    res.send("Server is running...")

})

const PORT = process.env.PORT || 

app.listen(5000, () => console.log('Server is running on port 5000'))